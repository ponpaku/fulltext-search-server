## System Version: 1.2.0
## File Version: 1.3.0

# 起動/運用依存の設定は .env に残す

# config.json の場所（未指定ならプロジェクト直下の config.json）
# CONFIG_PATH="config.json"

# 起動ポート（デフォルト: 80）
# PORT=80

# SSL証明書の配置ディレクトリ（lan-cert.pem / lan-key.pem）
# CERT_DIR="certs"

# 検索デバッグログの出力
# SEARCH_DEBUG=0

# 一時的に挙動を切り替えたい場合は env で上書き可能
# SEARCH_EXECUTION_MODE=thread
# SEARCH_PROCESS_SHARED=1

# それ以外のアプリ設定は config.json に記載してください
