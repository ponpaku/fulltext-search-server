## System Version: 1.2.0

# config.json でアプリ設定を管理します（環境変数は上書き用）
# CONFIG_PATH を指定すると config.json の場所を変更できます。
# CONFIG_PATH="config.json"

# 起動ポートを変えたい場合のみ指定（デフォルト: 80）
# PORT=80

# SSL証明書の配置ディレクトリ（lan-cert.pem / lan-key.pem を配置）
# CERT_DIR="certs"

# デバッグログを有効化したい場合のみ指定
# SEARCH_DEBUG=1

# 運用中の一時切替（必要な場合のみ設定）
# SEARCH_EXECUTION_MODE=thread
# SEARCH_PROCESS_SHARED=1
